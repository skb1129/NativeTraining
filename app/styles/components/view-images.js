import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  title: {
    fontSize: 35,
    padding: 15,
    fontWeight: 'bold',
    color: '#000',
  },
});

export default styles;
