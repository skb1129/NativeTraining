import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingTop: 20,
  },
  title: {
    fontSize: 35,
    padding: 15,
    fontWeight: 'bold',
    color: '#000',
  },
});

export default styles;
